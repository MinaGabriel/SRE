# Sentence Relevance Estimation (SRE)

Binary sentence relevance estimation for QA-style tasks.

Given a natural language question and a set of candidate sentences (e.g., retrieved from a corpus), this project trains and evaluates models that decide whether each candidate sentence is **useful** in answering the question (`Yes` / `No`). The pipeline combines:
